import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    btn: {
        backgroundColor: "#151f58",
        borderRadius: 64,
        padding: 8,
        width: 40,        
    },
    container:{
        marginBottom: 12,
    }
})